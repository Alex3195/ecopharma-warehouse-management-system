server:
  port: ${SERVER_PORT}

spring:
  datasource:
    url: ${SPRING_DB_URL}
    username: ${SPRING_DB_USERNAME}
    password: ${SPRING_DB_PASSWORD}
  jpa:
    show-sql: false
  liquibase:
    default-schema: ${LIQUIBASE_DEFAULT_SCHEMA}
