package uz.duol.ecopharmwarehouse.enums;

public enum ShipmentStatusEnum {
    CREATED, // Created shipment
    PICKED, // Picked shipment
    PACKED, // Packed shipment
    SHIPPED, // Shipped shipment
    DELIVERED, // Delivered shipment
    CANCELLED, // Cancelled shipment
    RETURNED, // Returned shipment
    PARTIALLY_PICKED, // Partially picked shipment
    PARTIALLY_PACKED, // Partially packed shipment
    PARTIALLY_SHIPPED, // Partially shipped shipment
    PARTIALLY_DELIVERED, // Partially delivered shipment
    PARTIALLY_RETURNED, // Partially returned shipment
    PARTIALLY_CANCELLED, // Partially cancelled shipment
    PARTIALLY_PICKED_PACKED, // Partially picked and packed shipment
    PARTIALLY_PICKED_SHIPPED, // Partially picked and shipped shipment
    PARTIALLY_PICKED_DELIVERED, // Partially picked and delivered shipment
    PARTIALLY_PICKED_RETURNED, // Partially picked and returned shipment
    PARTIALLY_PICKED_CANCELLED, // Partially picked and cancelled shipment
    PARTIALLY_PACKED_SHIPPED, // Partially packed and shipped shipment
    PARTIALLY_PACKED_DELIVERED, // Partially packed and delivered shipment
    PARTIALLY_PACKED_RETURNED, // Partially packed and returned shipment
    PARTIALLY_PACKED_CANCELLED, // Partially packed and cancelled shipment
    PARTIALLY_SHIPPED_DELIVERED, // Partially shipped and delivered shipment
    PARTIALLY_SHIPPED_RETURNED, // Partially shipped and returned shipment
    PARTIALLY_SHIPPED_CANCELLED, // Partially shipped and cancelled shipment
    PARTIALLY_DELIVERED_RETURNED, // Partially delivered and returned shipment
    PARTIALLY_DELIVERED_CANCELLED, // Partially delivered and cancelled shipment
    PARTIALLY_RETURNED_CANCELLED, // Partially returned and cancelled shipment
    PARTIALLY_PICKED_PACKED_SHIPPED, // Partially picked, packed and shipped shipment
    PARTIALLY_PICKED_PACKED_DELIVERED, // Partially picked, packed and delivered shipment
    PARTIALLY_PICKED_PACKED_RETURNED, // Partially picked, packed and returned shipment
    PARTIALLY_PICKED_PACKED_CANCELLED, // Partially picked, packed and cancelled shipment
    PARTIALLY_PICKED_SHIPPED_DELIVERED, // Partially picked, shipped and delivered shipment
}
